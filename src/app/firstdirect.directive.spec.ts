import { FirstdirectDirective } from './firstdirect.directive';

describe('FirstdirectDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstdirectDirective();
    expect(directive).toBeTruthy();
  });
});
